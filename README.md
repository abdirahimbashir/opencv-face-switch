# opencv-face-switch
OpenCV Face Switch is an innovative hands-free lighting system that uses computer vision to control an LED based on real-time face detection. When the system detects a face through the webcam, it automatically turns on an LED connected to an Arduino; when no face is present, the LED turns off.
